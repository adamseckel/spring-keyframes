{
  "name": "spring-keyframes",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/hemlok/spring-keyframes.git",
  "author": "Adam Seckel <adam.seckel@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "demo": "yarn build & cd ./demo && yarn dev",
    "release": "yarn build && lerna publish",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}
